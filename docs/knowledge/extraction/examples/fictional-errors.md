# Fictional Extraction Errors

Invalid extraction copies the paper conclusion, omits Section 5, calls the Source
Evidence, converts Evidence directly to accepted Claim, invents Concept **Lumen
Cause** from a heading, labels a translation as identity, infers `causes` because
two cards share a page, and adds them to Package **Mirage** as published.

Other failures include ChatGPT memory as Evidence, automatic summary as Claim,
hallucinated terminology, numeric confidence as truth, missing Citation, ignored
rights, automatic Diagnosis, and Recommendation. Every defect returns or blocks
the affected stage; none is corrected by silent editing.
