# Rollback

Rollback is a governed decision to return source or a distributed product to a
previously reviewed safe state. Identify the affected version, candidate, public
artifacts, compatibility consequences, and recovery route before acting.

This repository provides no deployment rollback automation. Destructive Git
operations, tag changes, release edits, or package-index actions require explicit
target confirmation, approval, and platform-specific procedures outside this
preparation guide.
